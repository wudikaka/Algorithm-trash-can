给定一个二维矩阵 matrix，以下类型的多个请求：  
计算其子矩形范围内元素的总和，该子矩阵的 左上角 为 (row1, col1) ，右下角 为 (row2, col2) 。
实现 NumMatrix 类：  
NumMatrix(int[][] matrix) 给定整数矩阵 matrix 进行初始化  
int sumRegion(int row1, int col1, int row2, int col2) 返回 左上角 (row1, col1) 、右下角 (row2, col2) 所描述的子矩阵的元素 总和 。  


***二维数组前缀和--模板***

- 我感觉这题目有问题。。。matrix已经有了，这里还说给它初始化用
我自己理解的整个过程：
// 1. 测试代码先准备好数据  
vector< vector< int >> matrix = {{3,0,1,4,2}, {5,6,3,2,1}, ...};  
// 2. new 操作会：  
//    - 分配内存  
//    - 自动调用构造函数 NumMatrix(matrix)  
//    - 构造函数执行完，对象就初始化好了  
NumMatrix* obj = new NumMatrix(matrix);  
// 3. 后面调用 sumRegion，用的是已经初始化好的 obj  
int result1 = obj->sumRegion(2, 1, 4, 3);  // 返回 8  
int result2 = obj->sumRegion(1, 1, 2, 2);  // 返回 11   
int result3 = obj->sumRegion(1, 2, 2, 4);  // 返回 12  

- 分析：
其实就是一维数组前缀和plus版，对每行分别求前缀和，求区间和时也是到每行去分别复原前缀和。  



```cpp
class NumMatrix {
public:
    vector<vector<int>> sums;
    NumMatrix(vector<vector<int>> &matrix)
    {
        int rowSize = matrix.size(); // 行数
        if (rowSize > 0)
        {
            int colSize = matrix[0].size(); // 列数
            sums.resize(rowSize, vector<int>(colSize + 1));
            for (int i = 0; i < rowSize; i++)
            {
                for (int j = 0; j < colSize; j++) // 给每行都搞个前缀和数组
                {
                    sums[i][j + 1] = sums[i][j] + matrix[i][j]; 
                }
            }
        }
    }

    int sumRegion(int row1, int col1, int row2, int col2) 
    {
        int sum = 0;
        for (int i = row1; i <= row2; i++)
        {
            sum += (sums[i][col2 + 1] - sums[i][col1]); // 分别到每行复原前缀和
        }
        return sum;
    }
};

/**
 * Your NumMatrix object will be instantiated and called as such:
 * NumMatrix* obj = new NumMatrix(matrix);
 * int param_1 = obj->sumRegion(row1,col1,row2,col2);
 */
```